server:
  port: 8888

spring:
  application:
    name: configserver

  cloud:
    config:
      server:
        git:
          uri: https://github.com/microservice-phungthanhtoan/config_server
          defaut-label: main
        bootstrap: false